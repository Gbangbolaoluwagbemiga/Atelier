{
  "name": "atelier-subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth",
    "deploy": "graph deploy atelier-secureflow --node https://subgraphs.thegraph.com/deploy --ipfs https://ipfs.thegraph.com",
    "deploy:studio": "graph deploy atelier-secureflow",
    "deploy:goldsky": "goldsky subgraph deploy atelier/v1 --path ."
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.81.0",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
